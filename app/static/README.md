# static

Served as-is at the site root: `static/logo.png` is `/logo.png`.

`logo.png` is the client's mark, printed in the corner of every exported
document. It is deliberately not committed with placeholder art — the document
header hides the image when the file is absent, so the export works without it
and picks the logo up the moment it is dropped in.
